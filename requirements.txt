# PDF text-layer extraction
pypdfium2>=4.30,<5

# Fallback for encrypted PDFs — opens more encryption variants than pypdfium2
# alone and can strip owner-only locks. Used by pdf_extractor as a second-try
# when pypdfium2 refuses a password-protected file.
pikepdf>=9.0,<10

# Web framework
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
python-multipart>=0.0.20,<1

# LLM client (Azure OpenAI uses the openai SDK)
openai>=1.55,<2

# HTTP client for the PaddleOCR upstream call (ocr_classifier)
httpx>=0.27,<1

# ocr_slip salary-slip parser: image rendering + OCR preprocessing + its
# requests-based Azure call
Pillow>=10.0,<12
numpy>=1.24
requests>=2.32,<3
urllib3<2

# Settings / models
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
python-dotenv>=1.0,<2
